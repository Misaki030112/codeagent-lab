# codeagent-lab

Private lab repository for local CodeAgent integration testing.

## Layout

- `go/` small Go snippets for `/code` and `/review`
- `web/` small TypeScript snippets
- `python/` small Python snippets
- `docs/TASKS.md` prompt ideas for webhook testing
Private lab repo for local CodeAgent integration testing
